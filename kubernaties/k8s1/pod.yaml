apiversion: v1
kind: pod
metadata:
  name: nginx
    labels:
      app: myapp
      env: devops
spec:
  containers:
    - name: nginx
      image: nginx
  port: 
    - containerPort : 80










# apiVersion: v1
# kind: Pod
# metadata:
#   name: nginx
# spec:
#   containers:
#   - name: nginx
#     image: nginx:1.14.2
#     ports:
#     - containerPort: 80
